# API keys — never commit credentials
api-keys.txt
nass-key.txt
noaa-token.txt
ebird-key.txt

# SSH keys
key
key.pub

# Snapshots — runtime cache and harvested iNat data, not for version control
# Pre-computed reference files (climate-normals, observed-temps) ARE committed
snapshots/cache/
snapshots/inat-*.json
